<section class="section">
  <div class="container" style="text-align:center; padding: 100px 20px;">
    <span class="eyebrow">404</span>
    <h1>Page not found</h1>
    <p class="text-muted" style="max-width: 480px; margin: 0 auto 24px;">The page you were looking for has moved or doesn't exist.</p>
    <a href="{{ routes.root_url }}" class="btn btn--primary">Back to home</a>
    <a href="{{ routes.all_products_collection_url }}" class="btn btn--outline" style="margin-left: 8px;">Shop all</a>
  </div>
</section>
