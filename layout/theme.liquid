<!doctype html>
<html lang="{{ request.locale.iso_code }}" class="no-js">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
    <meta name="theme-color" content="{{ settings.color_accent }}">
    <link rel="canonical" href="{{ canonical_url }}">

    {%- if settings.favicon != blank -%}
      <link rel="icon" type="image/png" href="{{ settings.favicon | image_url: width: 32, height: 32 }}">
    {%- endif -%}

    {%- unless settings.type_heading_font.system? and settings.type_body_font.system? -%}
      <link rel="preconnect" href="https://fonts.shopifycdn.com" crossorigin>
    {%- endunless -%}

    <title>
      {{ page_title }}
      {%- if current_tags %} &ndash; tagged "{{ current_tags | join: ', ' }}"{% endif -%}
      {%- if current_page != 1 %} &ndash; Page {{ current_page }}{% endif -%}
      {%- unless page_title contains shop.name %} &ndash; {{ shop.name }}{% endunless -%}
    </title>

    {%- if page_description -%}
      <meta name="description" content="{{ page_description | escape }}">
    {%- endif -%}

    {% render 'meta-tags' %}

    <script src="{{ 'theme.js' | asset_url }}" defer></script>
    <script src="{{ 'cart.js' | asset_url }}" defer></script>

    {{ content_for_header }}

    {%- liquid
      assign body_font = settings.type_body_font
      assign heading_font = settings.type_heading_font
      assign body_font_bold = body_font | font_modify: 'weight', 'bold'
      assign body_font_italic = body_font | font_modify: 'style', 'italic'
    -%}

    {% style %}
      {{ body_font | font_face: font_display: 'swap' }}
      {{ body_font_bold | font_face: font_display: 'swap' }}
      {{ body_font_italic | font_face: font_display: 'swap' }}
      {{ heading_font | font_face: font_display: 'swap' }}

      :root {
        --color-bg: {{ settings.color_background }};
        --color-text: {{ settings.color_text }};
        --color-muted: {{ settings.color_muted }};
        --color-accent: {{ settings.color_accent }};
        --color-accent-contrast: {{ settings.color_accent_contrast }};
        --color-border: {{ settings.color_border }};
        --color-card: {{ settings.color_card }};
        --color-surface: {{ settings.color_surface }};
        --color-ink: #0A0B0F;
        --color-ink-soft: #1B1E26;
        --color-success: #1F8A4C;
        --color-sale: #C72E1C;

        /* Accent in RGB for layered/tinted shadows + glows */
        --accent-rgb: 255, 74, 28;

        --radius-sm: 4px;
        --radius-md: 8px;
        --radius-lg: 14px;
        --radius-xl: 24px;
        --button-radius: {{ settings.button_radius }};

        --container: {{ settings.container_width }}px;
        --header-height: 76px;

        /* 8px-based spacing tokens */
        --space-1: 4px;
        --space-2: 8px;
        --space-3: 12px;
        --space-4: 16px;
        --space-6: 24px;
        --space-8: 32px;
        --space-12: 48px;
        --space-16: 64px;
        --space-20: 80px;
        --space-24: 96px;
        --space-32: 128px;
        --space-40: 160px;

        --font-body: {{ body_font.family }}, {{ body_font.fallback_families }};
        --font-heading: {{ heading_font.family }}, {{ heading_font.fallback_families }};
        --font-mono: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
        --weight-body: {{ body_font.weight }};
        --weight-bold: {{ body_font_bold.weight }};
        --weight-heading: {{ heading_font.weight }};

        /* Layered, color-tinted shadows (low opacity, depth via stacking) */
        --shadow-sm: 0 1px 2px rgba(10, 11, 15, 0.04), 0 1px 1px rgba(10, 11, 15, 0.06);
        --shadow-md: 0 4px 6px -1px rgba(10, 11, 15, 0.05), 0 10px 15px -3px rgba(10, 11, 15, 0.06);
        --shadow-lg: 0 20px 25px -5px rgba(10, 11, 15, 0.08), 0 8px 10px -6px rgba(10, 11, 15, 0.05);
        --shadow-xl: 0 25px 50px -12px rgba(10, 11, 15, 0.15);
        --shadow-glow: 0 0 0 1px rgba(var(--accent-rgb), 0.10), 0 12px 32px -8px rgba(var(--accent-rgb), 0.28), 0 4px 12px -2px rgba(10, 11, 15, 0.06);

        /* Motion */
        --ease-spring: cubic-bezier(0.22, 1, 0.36, 1);
        --ease-snap: cubic-bezier(0.16, 1, 0.3, 1);
        --ease-in-out: cubic-bezier(0.4, 0, 0.2, 1);
        --dur-fast: 150ms;
        --dur-base: 250ms;
        --dur-slow: 400ms;
      }
    {% endstyle %}

    <link rel="stylesheet" href="{{ 'theme.css' | asset_url }}">

    <script>
      document.documentElement.classList.remove('no-js');
      window.routes = {
        cart_add_url: '{{ routes.cart_add_url }}',
        cart_change_url: '{{ routes.cart_change_url }}',
        cart_update_url: '{{ routes.cart_update_url }}',
        cart_url: '{{ routes.cart_url }}',
        predictive_search_url: '{{ routes.predictive_search_url }}'
      };
      window.shopCurrency = '{{ cart.currency.iso_code }}';
    </script>
  </head>

  <body class="template-{{ template.name | handleize }}{% if template.suffix %} template-{{ template.suffix | handleize }}{% endif %}">
    <a class="skip-link" href="#MainContent">Skip to content</a>

    {% sections 'header-group' %}

    <main id="MainContent" class="main" role="main">
      {{ content_for_layout }}
    </main>

    {% sections 'footer-group' %}

    {% section 'cart-drawer' %}

    <div id="ToastContainer" class="toast-container" aria-live="polite" aria-atomic="true"></div>
  </body>
</html>
